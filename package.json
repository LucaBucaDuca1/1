{
  "name": "homeflix",
  "version": "1.0.0",
  "description": "my personal netflix clone for phone, desktop, and LG TV",
  "author": "zeloz",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "lg-tv-app"
  ],
  "scripts": {
    "setup": "npm install && cd server && npm install && cd ../client && npm install",
    "install-all": "npm run setup",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm run build && cd ../lg-tv-app && npm run build",
    "start": "cd server && npm start",
    "doctor": "node scripts/doctor.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
